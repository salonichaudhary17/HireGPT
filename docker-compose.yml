services:
  server:
    build:
      context: ./server
    container_name: hiregpt-server
    ports:
      - "8000:8000"
    env_file:
      - ./server/.env
    restart: unless-stopped

  client:
    build:
      context: ./client
    container_name: hiregpt-client
    ports:
      - "5173:5173"
    depends_on:
      - server
    restart: unless-stopped